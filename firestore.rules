rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Shared studio database for UNKE team
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
